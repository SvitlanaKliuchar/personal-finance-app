import {SignedIn, SignedOut, SignInButton, SignUpButtonButton, UserButton} from '@clerk/clerk-react'

export const Auth = () => {
    return <div className="sign-in-container">
        <SignedOut>

        </SignedOut>
        <SignedIn>
            
        </SignedIn>
    </div>
}